# Test-only dependencies. Nothing here is needed to build or run the server.
pytest>=8.0
# Optional: only needed for the tests marked `db`, which verify what actually
# reached PostgreSQL. Without it those tests skip.
psycopg[binary]>=3.1
