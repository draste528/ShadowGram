[pytest]
testpaths = .
addopts = -ra
xfail_strict = true
markers =
    db: needs SHADOWGRAM_TEST_DSN and the psycopg driver
    slow: takes a few seconds of wall clock
    characterization: pins current (sometimes wrong) behaviour so changes are noticed
